services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
      - ./backend/temp:/app/temp

  frontend:
    build: ./frontend-widget
    ports:
      - "5173:80"
    depends_on:
      - backend
